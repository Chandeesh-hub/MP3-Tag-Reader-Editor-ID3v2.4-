# MP3-Tag-Reader-Editor-ID3v2.4-
MP3TagX is a C-based command-line utility for reading and editing ID3v2.4 tag metadata embedded in MP3 audio files. It lets you inspect a song's Title, Artist, Album, Year, Composer, and Comment fields, or update any single tag in place — all without modifying the underlying audio data.
